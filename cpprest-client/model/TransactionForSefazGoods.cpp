/**
 * AvaTax Brazil
 * The Avatax-Brazil API exposes the most commonly services available for interacting with the AvaTax-Brazil services, allowing calculation of taxes, issuing electronic invoice documents and modifying existing transactions when allowed by tax authorities.  This API is exclusively for use by business with a physical presence in Brazil.
 *
 * OpenAPI spec version: 1.0
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */



#include "TransactionForSefazGoods.h"

namespace io {
namespace swagger {
namespace client {
namespace model {

TransactionForSefazGoods::TransactionForSefazGoods()
{
    m_HeaderIsSet = false;
    m_LinesIsSet = false;
    m_CalculatedTaxSummaryIsSet = false;
    
}

TransactionForSefazGoods::~TransactionForSefazGoods()
{
}

void TransactionForSefazGoods::validate()
{
    // TODO: implement validation
}

web::json::value TransactionForSefazGoods::toJson() const
{
    web::json::value val = web::json::value::object();

    if(m_HeaderIsSet)
    {
        val[U("header")] = ModelBase::toJson(m_Header);
    }
    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Lines )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        
        if(jsonArray.size() > 0)
        {
            val[U("lines")] = web::json::value::array(jsonArray);
        }
    }
    if(m_CalculatedTaxSummaryIsSet)
    {
        val[U("calculatedTaxSummary")] = ModelBase::toJson(m_CalculatedTaxSummary);
    }
    

    return val;
}

void TransactionForSefazGoods::fromJson(web::json::value& val)
{
    if(val.has_field(U("header")))
    {
        if(!val[U("header")].is_null())
        {
            std::shared_ptr<HeaderForGoods> newItem(new HeaderForGoods());
            newItem->fromJson(val[U("header")]);
            setHeader( newItem );
        }
        
    }
    {
        m_Lines.clear();
        std::vector<web::json::value> jsonArray;
        if(val.has_field(U("lines")))
        {
        for( auto& item : val[U("lines")].as_array() )
        {
            
            if(item.is_null())
            {
                m_Lines.push_back( std::shared_ptr<LineForSefazGoods>(nullptr) );
            }
            else
            {
                std::shared_ptr<LineForSefazGoods> newItem(new LineForSefazGoods());
                newItem->fromJson(item);
                m_Lines.push_back( newItem );
            }
            
        }
        }
    }
    if(val.has_field(U("calculatedTaxSummary")))
    {
        if(!val[U("calculatedTaxSummary")].is_null())
        {
            std::shared_ptr<CalculatedTaxSummaryForGoods> newItem(new CalculatedTaxSummaryForGoods());
            newItem->fromJson(val[U("calculatedTaxSummary")]);
            setCalculatedTaxSummary( newItem );
        }
        
    }
    
}

void TransactionForSefazGoods::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix[namePrefix.size() - 1] != U('.'))
    {
        namePrefix += U(".");
    }

    if(m_HeaderIsSet)
    {
        if (m_Header.get())
        {
            m_Header->toMultipart(multipart, U("header."));
        }
        
    }
    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Lines )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        
        if(jsonArray.size() > 0)
        {
            multipart->add(ModelBase::toHttpContent(namePrefix + U("lines"), web::json::value::array(jsonArray), U("application/json")));
        }
    }
    if(m_CalculatedTaxSummaryIsSet)
    {
        if (m_CalculatedTaxSummary.get())
        {
            m_CalculatedTaxSummary->toMultipart(multipart, U("calculatedTaxSummary."));
        }
        
    }
    
}

void TransactionForSefazGoods::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix[namePrefix.size() - 1] != U('.'))
    {
        namePrefix += U(".");
    }

    if(multipart->hasContent(U("header")))
    {
        if(multipart->hasContent(U("header")))
        {
            std::shared_ptr<HeaderForGoods> newItem(new HeaderForGoods());
            newItem->fromMultiPart(multipart, U("header."));
            setHeader( newItem );
        }
        
    }
    {
        m_Lines.clear();
        if(multipart->hasContent(U("lines")))
        {

        web::json::value jsonArray = web::json::value::parse(ModelBase::stringFromHttpContent(multipart->getContent(U("lines"))));
        for( auto& item : jsonArray.as_array() )
        {
            
            if(item.is_null())
            {
                m_Lines.push_back( std::shared_ptr<LineForSefazGoods>(nullptr) );
            }
            else
            {
                std::shared_ptr<LineForSefazGoods> newItem(new LineForSefazGoods());
                newItem->fromJson(item);
                m_Lines.push_back( newItem );
            }
            
        }
        }
    }
    if(multipart->hasContent(U("calculatedTaxSummary")))
    {
        if(multipart->hasContent(U("calculatedTaxSummary")))
        {
            std::shared_ptr<CalculatedTaxSummaryForGoods> newItem(new CalculatedTaxSummaryForGoods());
            newItem->fromMultiPart(multipart, U("calculatedTaxSummary."));
            setCalculatedTaxSummary( newItem );
        }
        
    }
    
}


std::shared_ptr<HeaderForGoods> TransactionForSefazGoods::getHeader() const
{
    return m_Header;
}
void TransactionForSefazGoods::setHeader(std::shared_ptr<HeaderForGoods> value)
{
    m_Header = value;
    m_HeaderIsSet = true;
}
bool TransactionForSefazGoods::headerIsSet() const
{
    return m_HeaderIsSet;
}
void TransactionForSefazGoods::unsetHeader()
{
    m_HeaderIsSet = false;
}
std::vector<std::shared_ptr<LineForSefazGoods>>& TransactionForSefazGoods::getLines()
{
    return m_Lines;
}
bool TransactionForSefazGoods::linesIsSet() const
{
    return m_LinesIsSet;
}
void TransactionForSefazGoods::unsetLines()
{
    m_LinesIsSet = false;
}
std::shared_ptr<CalculatedTaxSummaryForGoods> TransactionForSefazGoods::getCalculatedTaxSummary() const
{
    return m_CalculatedTaxSummary;
}
void TransactionForSefazGoods::setCalculatedTaxSummary(std::shared_ptr<CalculatedTaxSummaryForGoods> value)
{
    m_CalculatedTaxSummary = value;
    m_CalculatedTaxSummaryIsSet = true;
}
bool TransactionForSefazGoods::calculatedTaxSummaryIsSet() const
{
    return m_CalculatedTaxSummaryIsSet;
}
void TransactionForSefazGoods::unsetCalculatedTaxSummary()
{
    m_CalculatedTaxSummaryIsSet = false;
}

}
}
}
}

