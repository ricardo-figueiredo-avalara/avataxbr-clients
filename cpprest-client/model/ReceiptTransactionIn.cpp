/**
 * AvaTax Brazil
 * The Avatax-Brazil API exposes the most commonly services available for interacting with the AvaTax-Brazil services, allowing calculation of taxes, issuing electronic invoice documents and modifying existing transactions when allowed by tax authorities.  This API is exclusively for use by business with a physical presence in Brazil.
 *
 * OpenAPI spec version: 1.0
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */



#include "ReceiptTransactionIn.h"

namespace io {
namespace swagger {
namespace client {
namespace model {

ReceiptTransactionIn::ReceiptTransactionIn()
{
    
}

ReceiptTransactionIn::~ReceiptTransactionIn()
{
}

void ReceiptTransactionIn::validate()
{
    // TODO: implement validation
}

web::json::value ReceiptTransactionIn::toJson() const
{
    web::json::value val = web::json::value::object();

    val[U("header")] = ModelBase::toJson(m_Header);
    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Lines )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        val[U("lines")] = web::json::value::array(jsonArray);
            }
    

    return val;
}

void ReceiptTransactionIn::fromJson(web::json::value& val)
{
    std::shared_ptr<PayRecHeader> newHeader(new PayRecHeader());
    newHeader->fromJson(val[U("header")]);
    setHeader( newItem );
    {
        m_Lines.clear();
        std::vector<web::json::value> jsonArray;
                for( auto& item : val[U("lines")].as_array() )
        {
            
            if(item.is_null())
            {
                m_Lines.push_back( std::shared_ptr<PaymentLinesIn>(nullptr) );
            }
            else
            {
                std::shared_ptr<PaymentLinesIn> newItem(new PaymentLinesIn());
                newItem->fromJson(item);
                m_Lines.push_back( newItem );
            }
            
        }
    }
    
}

void ReceiptTransactionIn::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix[namePrefix.size() - 1] != U('.'))
    {
        namePrefix += U(".");
    }

    m_Header->toMultipart(multipart, U("header."));
    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Lines )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        multipart->add(ModelBase::toHttpContent(namePrefix + U("lines"), web::json::value::array(jsonArray), U("application/json")));
            }
    
}

void ReceiptTransactionIn::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix[namePrefix.size() - 1] != U('.'))
    {
        namePrefix += U(".");
    }

    std::shared_ptr<PayRecHeader> newHeader(new PayRecHeader());
    newHeader->fromMultiPart(multipart, U("header."));
    setHeader( newHeader );
    {
        m_Lines.clear();
        
        web::json::value jsonArray = web::json::value::parse(ModelBase::stringFromHttpContent(multipart->getContent(U("lines"))));
        for( auto& item : jsonArray.as_array() )
        {
            
            if(item.is_null())
            {
                m_Lines.push_back( std::shared_ptr<PaymentLinesIn>(nullptr) );
            }
            else
            {
                std::shared_ptr<PaymentLinesIn> newItem(new PaymentLinesIn());
                newItem->fromJson(item);
                m_Lines.push_back( newItem );
            }
            
        }
    }
    
}


std::shared_ptr<PayRecHeader> ReceiptTransactionIn::getHeader() const
{
    return m_Header;
}
void ReceiptTransactionIn::setHeader(std::shared_ptr<PayRecHeader> value)
{
    m_Header = value;
    
}
std::vector<std::shared_ptr<PaymentLinesIn>>& ReceiptTransactionIn::getLines()
{
    return m_Lines;
}

}
}
}
}

